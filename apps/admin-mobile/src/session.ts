import*as SecureStore from'expo-secure-store';const ACCESS='sugat.admin.access',REFRESH='sugat.admin.refresh';export const saveSession=(session:{accessToken:string;refreshToken:string})=>Promise.all([SecureStore.setItemAsync(ACCESS,session.accessToken),SecureStore.setItemAsync(REFRESH,session.refreshToken)]);export const accessToken=()=>SecureStore.getItemAsync(ACCESS);export const refreshToken=()=>SecureStore.getItemAsync(REFRESH);export const clearSession=()=>Promise.all([SecureStore.deleteItemAsync(ACCESS),SecureStore.deleteItemAsync(REFRESH)]);
