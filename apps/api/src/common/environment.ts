export function allowedOrigins(){return(process.env.CORS_ORIGINS??process.env.WEB_ORIGIN??'').split(',').map(v=>v.trim()).filter(Boolean)}
export function validateEnvironment(){const required=['DATABASE_URL','JWT_ACCESS_SECRET','JWT_REFRESH_SECRET'];if(process.env.NODE_ENV==='production')required.push('REDIS_URL','CORS_ORIGINS');const missing=required.filter(k=>!process.env[k]);if(missing.length)throw new Error(`Missing required environment variables: ${missing.join(', ')}`);for(const key of['JWT_ACCESS_SECRET','JWT_REFRESH_SECRET'])if((process.env[key]?.length??0)<32)throw new Error(`${key} must contain at least 32 characters`);}
